% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve_moment_design.R
\name{solve_moment_design}
\alias{solve_moment_design}
\title{Evaluate or optimize a Section 4 moment-selection design}
\usage{
solve_moment_design(
  Lambda,
  Sigma,
  Omega = NULL,
  omega = NULL,
  biased_moments = NULL,
  bias_weights = NULL,
  k = NULL,
  kappa = NULL,
  norm = c("linf", "l2", "l1"),
  W = NULL,
  W_mask = NULL,
  W_masks = NULL,
  moment_mask = NULL,
  moment_sets = NULL,
  optimize_W = is.null(W),
  cvxr_solver = NULL,
  cvxr_args = list(),
  max_sign_patterns = 4096L,
  reconstruct_W = TRUE,
  tol = 1e-08
)
}
\arguments{
\item{Lambda}{Numeric \code{m} by \code{d} Jacobian of moments with respect to parameters.}

\item{Sigma}{Numeric \code{m} by \code{m} covariance matrix for the moment vector.}

\item{Omega}{Numeric \code{q} by \code{d} target-sensitivity matrix. For a scalar target,
you may pass \code{omega} instead.}

\item{omega}{Optional length-\code{d} vector used as \code{Omega = matrix(omega, 1)}.}

\item{biased_moments}{Integer or logical index for moments that may be biased
(\code{I^c} in the paper). Defaults to all moments.}

\item{bias_weights}{Optional nonnegative length-\code{m} vector. These rescale the
bias radius by multiplying the biased columns of \code{Omega \%*\% Gamma}.}

\item{k, kappa}{Optional aliases for \code{bias_weights}. \code{kappa} matches the
weighted-bias notation \verb{|b_j| <= kappa[j] * B}.}

\item{norm}{Bias ambiguity norm: \code{"linf"}, \code{"l2"}, or \code{"l1"}.}

\item{W}{Optional user-supplied weighting matrix. Required when
\code{optimize_W = FALSE}.}

\item{W_mask}{Optional logical \code{m} by \code{m} mask of allowed entries in \code{W}.}

\item{W_masks}{Optional list of \code{W_mask} matrices. When supplied and
\code{optimize_W = TRUE}, the function solves each mask and selects the best
minimax-regret solution.}

\item{moment_mask}{Optional logical length-\code{m} vector of moments that may enter.}

\item{moment_sets}{Optional list of integer moment sets to compare.}

\item{optimize_W}{Logical. If \code{FALSE}, evaluate the supplied \code{W}; if \code{TRUE},
optimize over the convex equivalent linear GMM estimator under the mask(s).}

\item{cvxr_solver}{Optional CVXR solver name, e.g. \code{"ECOS"}, \code{"CLARABEL"}, or
\code{"SCS"} depending on what is installed.}

\item{cvxr_args}{Optional named list passed to \code{CVXR::solve()}.}

\item{max_sign_patterns}{Maximum sign patterns for exact multivariate
\code{norm = "linf"} bias calculations.}

\item{reconstruct_W}{Logical. If \code{TRUE}, reconstructs one weighting matrix that
implements the optimized \code{Gamma} and respects the selected mask when possible.}

\item{tol}{Numerical tolerance.}
}
\value{
A list of class \code{moment_design}.
}
\description{
Implements the Section 4 GMM/moment-selection regret formulas. With a
user-supplied \code{W} and \code{optimize_W = FALSE}, the function evaluates
\code{alpha_Omega(W, Sigma)}, \verb{beta_l,Omega(W)}, and the corresponding GMM linear
expansion. With \code{optimize_W = TRUE}, it solves the convex equivalent problem
over the linear GMM influence matrix \code{Gamma}, subject to the supplied \code{W_mask},
\code{W_masks}, \code{moment_mask}, or \code{moment_sets}.
}
