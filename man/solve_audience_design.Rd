% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve_audience_design.R
\name{solve_audience_design}
\alias{solve_audience_design}
\alias{solve_audience_regret_design}
\title{Solve the Section 4.1.1 audience-regret design problem on a grid of B values}
\usage{
solve_audience_design(
  Sigma_obs,
  v2,
  n_total,
  omega,
  B_grid,
  costs = NULL,
  bias_weights = NULL,
  Lambda_obs = NULL,
  Lambda_exp = NULL,
  h = length(v2),
  x_max = NULL,
  feasible_sets = NULL,
  selection_constraints = NULL,
  min_experiments = 0L,
  cvxr_solver = NULL,
  cvxr_args = list(),
  max_sets = 200000L,
  sample_floor = 1e-08,
  tol = 1e-07,
  c = NULL,
  k = NULL,
  kappa = NULL
)

solve_audience_regret_design(
  Sigma_obs,
  v2,
  n_total,
  omega,
  B_grid,
  costs = NULL,
  bias_weights = NULL,
  Lambda_obs = NULL,
  Lambda_exp = NULL,
  h = length(v2),
  x_max = NULL,
  feasible_sets = NULL,
  selection_constraints = NULL,
  min_experiments = 0L,
  cvxr_solver = NULL,
  cvxr_args = list(),
  max_sets = 200000L,
  sample_floor = 1e-08,
  tol = 1e-07,
  c = NULL,
  k = NULL,
  kappa = NULL
)
}
\arguments{
\item{Sigma_obs}{Numeric \code{p} by \code{p} covariance matrix for the observational
estimators.}

\item{v2}{Numeric length-\code{p} vector. The experimental estimator for coordinate
\code{j} has variance \code{v2[j] / n_j}.}

\item{n_total}{Positive scalar budget \code{n}. If all entries of \code{costs} are one,
this is the total experimental sample size. Otherwise it is the budget in
the constraint \code{sum(costs * n_opt) = n_total}.}

\item{omega}{Numeric length-\code{p} sensitivity vector. For a nonlinear estimand,
use the gradient evaluated at the design-stage estimate.}

\item{B_grid}{Numeric vector of nonnegative prior-scale values \code{B_g}.}

\item{costs}{Optional positive length-\code{p} vector of per-unit costs. Defaults to
one for each coordinate.}

\item{bias_weights}{Optional nonnegative length-\code{p} vector. Defaults to one for
each coordinate. Values other than one implement the weighted ambiguity set
\verb{|b_j| <= bias_weights[j] * B}.}

\item{Lambda_obs}{Optional \code{p_o} by \code{d} loading matrix for observational
reported statistics. Defaults to the identity matrix.}

\item{Lambda_exp}{Optional \code{p_e} by \code{d} loading matrix for experimental
reported statistics. Defaults to the identity matrix.}

\item{h}{Integer upper bound on the number of selected experiments. Defaults
to \code{length(omega)}.}

\item{x_max}{Optional length-\code{p} vector of zeros and ones indicating which
experiments are eligible. Defaults to one for each coordinate.}

\item{feasible_sets}{Optional list of integer vectors. Each vector gives a
feasible selected set using 1-based indices. When supplied, the solver
selects one member of this list after applying \code{h}, \code{x_max},
\code{min_experiments}, and \code{selection_constraints}.}

\item{selection_constraints}{Optional list with entries \code{A}, \code{sense}, and
\code{rhs}, defining additional linear constraints on \code{x}. Senses may be \code{"<="},
\code{">="}, \code{"<"}, \code{">"}, or \code{"="}.}

\item{min_experiments}{Integer lower bound on the number of selected
experiments. Defaults to zero.}

\item{cvxr_solver}{Optional CVXR solver name, e.g. \code{"CLARABEL"}, \code{"ECOS"},
or \code{"SCS"} depending on what is installed.}

\item{cvxr_args}{Optional named list passed to \code{CVXR::solve()}.}

\item{max_sets}{Maximum number of feasible experiment sets to enumerate when
using the non-Gurobi solver.}

\item{sample_floor}{Small positive lower bound for selected-arm sample-size
variables in the conic solve. This avoids numerical division by zero in
quadratic-over-linear constraints.}

\item{tol}{Numeric tolerance used for diagnostic warnings.}

\item{c}{Alias for \code{costs}, included for script compatibility.}

\item{k, kappa}{Aliases for \code{bias_weights}, included for script compatibility.
\code{kappa} matches the notation in the paper.}
}
\value{
An object of class \code{audience_design}.
}
\description{
\code{solve_audience_design()} implements the finite-grid profiled posterior-risk
program in Appendix D.1. It first computes the oracle posterior risk
\code{P_star(B_g)} at each supplied grid value and the large-B bias benchmark
\code{beta_star}. It then chooses one experiment set and one sample allocation that
minimize the maximum posterior-risk ratio over the grid, together with the
large-B bias-ratio constraint.

By default the function uses the same direct-parameter design as
\code{\link[=solve_minimax_design]{solve_minimax_design()}}, with \code{Lambda_obs = I} and \code{Lambda_exp = I}. For a
more general linear reported-statistic problem, pass \code{Lambda_obs} and
\code{Lambda_exp}; the calibration constraint is
\code{t(Lambda_obs) \%*\% a_obs + t(Lambda_exp[E, ]) \%*\% a_exp = omega}.

Weighted bias radii are supported through \code{bias_weights}, \code{kappa}, or \code{k}.
For the weighted l-infinity ambiguity set \verb{|b_l| <= bias_weights[l] * B}, the
dual radius used in Appendix D.1 is \verb{sum_l bias_weights[l] * |a_obs_l|}.
}
