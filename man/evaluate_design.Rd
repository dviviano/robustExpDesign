% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve-variance-design.R
\name{evaluate_design}
\alias{evaluate_design}
\title{Evaluate variance, bias, and allocation for a supplied design}
\usage{
evaluate_design(
  Sigma_obs,
  v2,
  n_total,
  omega,
  x,
  gamma = NULL,
  a_exp = NULL,
  costs = NULL,
  bias_weights = NULL,
  gamma_lower = NULL,
  gamma_upper = NULL,
  a_exp_lower = NULL,
  a_exp_upper = NULL,
  force_gamma_unit_interval = TRUE,
  n_min = 0,
  n_opt = NULL,
  zero_tol = 1e-09,
  psd_tol = 1e-10,
  psd_action = base::c("error", "project"),
  tol = 1e-10,
  c = NULL,
  k = NULL
)
}
\arguments{
\item{Sigma_obs}{Numeric `p` by `p` covariance matrix for the observational
estimators.}

\item{v2}{Numeric length-`p` vector. The experimental estimator for coordinate
`j` has variance `v2[j] / n_j`.}

\item{n_total}{Positive scalar budget `n`. If all entries of `costs` are one,
this is the total experimental sample size. Otherwise it is the budget in
the constraint `sum(costs * n_opt) = n_total`.}

\item{omega}{Numeric length-`p` sensitivity vector. For a nonlinear estimand,
use the gradient evaluated at the design-stage estimate.}

\item{x}{Numeric or logical length-`p` selection vector.}

\item{gamma}{Numeric length-`p` shrinkage vector. Entries for unselected
experiments are multiplied by zero through `s = x * gamma`. In general-a
mode, this can be supplied as a convenience and is translated into
`a_exp = x * omega * gamma`.}

\item{a_exp}{Optional length-`p` vector of direct experimental linear weights
for use when `force_gamma_unit_interval = FALSE`.}

\item{costs}{Optional positive length-`p` vector of per-unit costs. Defaults to
one for each coordinate.}

\item{bias_weights}{Optional nonnegative length-`p` vector. Defaults to one for
each coordinate. Values other than one implement the weighted ambiguity set
`|b_j| <= bias_weights[j] * B`.}

\item{gamma_lower, gamma_upper}{Optional length-`p` vectors giving coordinate
bounds on `gamma`. Defaults are zero and one. Set both to one to use only
experimental estimates when selected. When `force_gamma_unit_interval =
FALSE` and `a_exp_lower`/`a_exp_upper` are not supplied, these bounds are
translated into direct experimental weight bounds through `a_exp = omega *
gamma`.}

\item{a_exp_lower, a_exp_upper}{Optional length-`p` vectors giving direct lower
and upper bounds for experimental linear weights in the general
identity-Lambda formulation used when `force_gamma_unit_interval = FALSE`.}

\item{force_gamma_unit_interval}{Logical. If `TRUE`, require
`0 <= gamma_lower <= gamma_upper <= 1`, preserving the original shrinkage
interpretation. If `FALSE`, solve the general identity-Lambda linear-weight
formulation over `a_exp`; this currently requires Gurobi.}

\item{n_min}{Optional nonnegative lower bound on the sample size allocated to
each selected experiment when `n_opt` is not supplied.}

\item{n_opt}{Optional explicit length-`p` sample allocation. If supplied,
`evaluate_design()` uses this allocation directly instead of profiling over
sample sizes.}

\item{zero_tol}{Relative tolerance used only to recognize a mathematically
zero bias oracle. It is applied against a homogeneous bias scale.}

\item{psd_tol}{Relative eigenvalue tolerance for covariance validation.}

\item{psd_action}{Either `"error"` or `"project"`. Corrections beyond
floating-point roundoff require explicit `"project"`; every projection is
reported in the fitted object.}

\item{tol}{Numeric tolerance used for diagnostic warnings.}

\item{c}{Alias for `costs`, included for script compatibility.}

\item{k}{Alias for `bias_weights`, included for script compatibility.}
}
\value{
A list with `s`, `gamma`, `a_exp`, `a_obs`, `n_opt`, `alpha`, and
  `beta`.
}
\description{
Computes the variance index `alpha(s)`, the bias index `beta(s)`, and the
cost-aware allocation for user-supplied selection and shrinkage vectors.
}
