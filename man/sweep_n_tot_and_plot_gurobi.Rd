% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sweep_n_total.R
\name{sweep_n_tot_and_plot_gurobi}
\alias{sweep_n_tot_and_plot_gurobi}
\title{Sweep total sample size and create design plots}
\usage{
sweep_n_tot_and_plot_gurobi(
  Sigma_obs,
  v2,
  omega,
  n_grid,
  costs = NULL,
  bias_weights = NULL,
  x_max = NULL,
  h_multi,
  h_single = 1L,
  BO_experiments = TRUE,
  ncor = 1L,
  solver = c("auto", "gurobi", "quadprog"),
  gurobi_params = list(),
  max_sets = 200000L,
  experiment_names = NULL,
  plot_total_n = FALSE,
  feasible_sets = NULL,
  selection_constraints = NULL,
  min_experiments = 0L,
  gamma_lower = NULL,
  gamma_upper = NULL,
  c = NULL,
  k = NULL
)
}
\arguments{
\item{Sigma_obs}{Numeric \code{p} by \code{p} covariance matrix for the observational
estimators.}

\item{v2}{Numeric length-\code{p} vector. The experimental estimator for coordinate
\code{j} has variance \code{v2[j] / n_j}.}

\item{omega}{Numeric length-\code{p} sensitivity vector. For a nonlinear estimand,
use the gradient evaluated at the design-stage estimate.}

\item{n_grid}{Numeric vector of positive budget values.}

\item{costs}{Optional positive length-\code{p} vector of per-unit costs. Defaults to
one for each coordinate.}

\item{bias_weights}{Optional nonnegative length-\code{p} vector. Defaults to one for
each coordinate. Values other than one implement the weighted ambiguity set
\verb{|b_j| <= bias_weights[j] * B}.}

\item{x_max}{Optional length-\code{p} vector of zeros and ones indicating which
experiments are eligible. Defaults to one for each coordinate.}

\item{h_multi}{Upper bound used when \code{BO_experiments = TRUE}.}

\item{h_single}{Upper bound for the single-count run.}

\item{BO_experiments}{Logical. If \code{TRUE}, run both \code{h_single} and \code{h_multi}.}

\item{ncor}{Number of cores.}

\item{solver}{One of \code{"auto"}, \code{"gurobi"}, or \code{"quadprog"}. \code{"quadprog"} avoids
Gurobi by enumerating feasible experiment sets and solving the convex
continuous subproblems with \code{quadprog}.}

\item{gurobi_params}{Optional named list of parameters passed to the Gurobi R
optimizer when \code{solver = "gurobi"}.}

\item{max_sets}{Maximum number of feasible experiment sets to enumerate when
using the non-Gurobi solver.}

\item{experiment_names}{Optional labels.}

\item{plot_total_n}{Logical. If \code{TRUE}, plot allocation levels rather than
allocation shares.}

\item{feasible_sets}{Optional list of integer vectors. Each vector gives a
feasible selected set using 1-based indices. When supplied, the solver
selects one member of this list after applying \code{h}, \code{x_max},
\code{min_experiments}, and \code{selection_constraints}.}

\item{selection_constraints}{Optional list with entries \code{A}, \code{sense}, and
\code{rhs}, defining additional linear constraints on \code{x}. Senses may be \code{"<="},
\code{">="}, \code{"<"}, \code{">"}, or \code{"="}.}

\item{min_experiments}{Integer lower bound on the number of selected
experiments. Defaults to zero.}

\item{gamma_lower, gamma_upper}{Optional length-\code{p} vectors giving coordinate
bounds on \code{gamma}. Defaults are zero and one. Set both to one to use only
experimental estimates when selected.}

\item{c}{Alias for \code{costs}, included for script compatibility.}

\item{k}{Alias for \code{bias_weights}, included for script compatibility.}
}
\value{
Output from \code{\link[=sweep_n_total]{sweep_n_total()}}.
}
\description{
Compatibility wrapper around \code{\link[=sweep_n_total]{sweep_n_total()}}. This function name is retained
for scripts using the Gurobi-only API while the \code{solver} argument
also allows non-Gurobi execution.
}
