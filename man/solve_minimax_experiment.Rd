% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solve_minimax_experiment.R
\name{solve_minimax_experiment}
\alias{solve_minimax_experiment}
\title{Solve the minimax design problem without Gurobi}
\usage{
solve_minimax_experiment(
  Sigma_obs,
  v2,
  n_total,
  omega,
  c = NULL,
  k = NULL,
  kappa = NULL,
  h = length(omega),
  x_max = NULL,
  feasible_sets = NULL,
  selection_constraints = NULL,
  min_experiments = 0L,
  gamma_lower = NULL,
  gamma_upper = NULL,
  max_sets = 200000L,
  tol_bisect = 1e-07,
  bisect_iter = 80L,
  qp_ridge = 1e-10,
  tol_ratio = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{Sigma_obs}{Numeric \code{p} by \code{p} covariance matrix for the observational
estimators.}

\item{v2}{Numeric length-\code{p} vector. The experimental estimator for coordinate
\code{j} has variance \code{v2[j] / n_j}.}

\item{n_total}{Positive scalar budget \code{n}. If all entries of \code{costs} are one,
this is the total experimental sample size. Otherwise it is the budget in
the constraint \code{sum(costs * n_opt) = n_total}.}

\item{omega}{Numeric length-\code{p} sensitivity vector. For a nonlinear estimand,
use the gradient evaluated at the design-stage estimate.}

\item{c}{Alias for \code{costs}, included for script compatibility.}

\item{k, kappa}{Aliases for \code{bias_weights}, included for script compatibility.}

\item{h}{Integer upper bound on the number of selected experiments. Defaults
to \code{length(omega)}.}

\item{x_max}{Optional length-\code{p} vector of zeros and ones indicating which
experiments are eligible. Defaults to one for each coordinate.}

\item{feasible_sets}{Optional list of integer vectors. Each vector gives a
feasible selected set using 1-based indices. When supplied, the solver
selects one member of this list after applying \code{h}, \code{x_max},
\code{min_experiments}, and \code{selection_constraints}.}

\item{selection_constraints}{Optional list with entries \code{A}, \code{sense}, and
\code{rhs}, defining additional linear constraints on \code{x}. Senses may be \code{"<="},
\code{">="}, \code{"<"}, \code{">"}, or \code{"="}.}

\item{min_experiments}{Integer lower bound on the number of selected
experiments. Defaults to zero.}

\item{gamma_lower, gamma_upper}{Optional length-\code{p} vectors giving coordinate
bounds on \code{gamma}. Defaults are zero and one. Set both to one to use only
experimental estimates when selected.}

\item{max_sets}{Maximum number of feasible experiment sets to enumerate.
Retained for compatibility with the stand-alone implementation. The wrapper
passes this value to \code{\link[=solve_minimax_design]{solve_minimax_design()}} when supported by the
non-Gurobi backend.}

\item{tol_bisect}{Numeric tolerance for bisection steps. Retained for
compatibility with the stand-alone implementation.}

\item{bisect_iter}{Integer maximum number of bisection iterations. Retained
for compatibility with the stand-alone implementation.}

\item{qp_ridge}{Small nonnegative ridge added to quadratic programs for
numerical stability. Retained for compatibility with the stand-alone
implementation.}

\item{tol_ratio}{Numeric tolerance used when checking whether normalized
regret ratios are below one because of numerical error.}

\item{verbose}{Logical; retained for compatibility. The current implementation
is silent apart from warnings.}
}
\value{
An object of class \code{minimax_design}.
}
\description{
Compatibility wrapper for the stand-alone script name supplied with early
versions of the project. This calls \code{\link[=solve_minimax_design]{solve_minimax_design()}} with
\code{solver = "quadprog"}.
}
