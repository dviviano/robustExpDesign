% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sweep-n-total.R
\name{sweep_n_total}
\alias{sweep_n_total}
\title{Sweep total budget values and collect design diagnostics}
\usage{
sweep_n_total(
  Sigma_obs,
  v2,
  omega,
  n_grid,
  h_values = 1L,
  costs = NULL,
  bias_weights = NULL,
  x_max = NULL,
  feasible_sets = NULL,
  selection_constraints = NULL,
  min_experiments = 0L,
  n_min = 0,
  gamma_lower = NULL,
  gamma_upper = NULL,
  a_exp_lower = NULL,
  a_exp_upper = NULL,
  force_gamma_unit_interval = TRUE,
  experiment_names = NULL,
  include_variance_design = TRUE,
  make_plots = TRUE,
  plot_total_n = FALSE,
  n_cores = 1L,
  solver = base::c("auto", "gurobi", "quadprog"),
  gurobi_params = list(),
  max_sets = 200000L,
  tol_bisect = 1e-07,
  bisect_iter = 80L,
  qp_ridge = 1e-10,
  zero_tol = 1e-09,
  psd_tol = 1e-10,
  psd_action = base::c("error", "project"),
  c = NULL,
  k = NULL
)
}
\arguments{
\item{Sigma_obs}{Numeric `p` by `p` covariance matrix for the observational
estimators.}

\item{v2}{Numeric length-`p` vector. The experimental estimator for coordinate
`j` has variance `v2[j] / n_j`.}

\item{omega}{Numeric length-`p` sensitivity vector. For a nonlinear estimand,
use the gradient evaluated at the design-stage estimate.}

\item{n_grid}{Numeric vector of positive budget values.}

\item{h_values}{Integer vector of experiment-count upper bounds.}

\item{costs}{Optional positive length-`p` vector of per-unit costs. Defaults to
one for each coordinate.}

\item{bias_weights}{Optional nonnegative length-`p` vector. Defaults to one for
each coordinate. Values other than one implement the weighted ambiguity set
`|b_j| <= bias_weights[j] * B`.}

\item{x_max}{Optional length-`p` vector of zeros and ones indicating which
experiments are eligible. Defaults to one for each coordinate.}

\item{feasible_sets}{Optional list of integer vectors. Each vector gives a
feasible selected set using 1-based indices. When supplied, the solver
selects one member of this list after applying `h`, `x_max`,
`min_experiments`, and `selection_constraints`.}

\item{selection_constraints}{Optional list with entries `A`, `sense`, and
`rhs`, defining additional linear constraints on `x`. Senses may be `"<="`,
`">="`, `"<"`, `">"`, or `"="`.}

\item{min_experiments}{Integer lower bound on the number of selected
experiments. Defaults to zero.}

\item{n_min}{Optional nonnegative lower bound on the sample size allocated to
each selected experiment. Defaults to zero, which preserves the profiled
Neyman-allocation implementation.}

\item{gamma_lower, gamma_upper}{Optional length-`p` vectors giving coordinate
bounds on `gamma`. Defaults are zero and one. Set both to one to use only
experimental estimates when selected. When `force_gamma_unit_interval =
FALSE` and `a_exp_lower`/`a_exp_upper` are not supplied, these bounds are
translated into direct experimental weight bounds through `a_exp = omega *
gamma`.}

\item{a_exp_lower, a_exp_upper}{Optional length-`p` vectors giving direct lower
and upper bounds for experimental linear weights in the general
identity-Lambda formulation used when `force_gamma_unit_interval = FALSE`.}

\item{force_gamma_unit_interval}{Logical. If `TRUE`, require
`0 <= gamma_lower <= gamma_upper <= 1`, preserving the original shrinkage
interpretation. If `FALSE`, solve the general identity-Lambda linear-weight
formulation over `a_exp`; this currently requires Gurobi.}

\item{experiment_names}{Optional character vector of coordinate labels.}

\item{include_variance_design}{Logical. If `TRUE`, also runs
[solve_variance_design()].}

\item{make_plots}{Logical. If `TRUE`, returns `ggplot2` plots when `ggplot2`
is installed.}

\item{plot_total_n}{Logical. If `TRUE`, allocation plots use `n_opt`; otherwise
they use the allocation share among selected experiments.}

\item{n_cores}{Integer number of cores for non-Windows parallel execution.}

\item{solver}{One of `"auto"`, `"gurobi"`, or `"quadprog"`. `"quadprog"` avoids
Gurobi by enumerating feasible experiment sets and solving the convex
continuous subproblems with `quadprog`.}

\item{gurobi_params}{Optional named list of parameters passed to the Gurobi R
optimizer when `solver = "gurobi"`.}

\item{max_sets}{Maximum number of feasible experiment sets to enumerate when
using the non-Gurobi solver.}

\item{tol_bisect, bisect_iter}{Controls for the bisection used by the non-Gurobi
minimax solver.}

\item{qp_ridge}{Relative, dimensionless ridge added after normalizing the
quadratic-program objective. This preserves target-unit invariance while
ensuring positive-definiteness for `quadprog`.}

\item{zero_tol}{Relative tolerance for homogeneous zero-risk diagnostics.}

\item{psd_tol}{Relative covariance eigenvalue tolerance.}

\item{psd_action}{Either `"error"` or `"project"`. Corrections beyond
floating-point roundoff require explicit `"project"`; projections are
reported in fitted objects.}

\item{c}{Alias for `costs`, included for script compatibility.}

\item{k}{Alias for `bias_weights`, included for script compatibility.}
}
\value{
A list with `data`, `plots`, and `fits`.
}
\description{
Runs [solve_minimax_design()] over a grid of total budgets and experiment-count
limits. Optionally also computes the variance-optimal benchmark. The returned
data frames are suitable for allocation, shrinkage, and regret diagnostics.
}
